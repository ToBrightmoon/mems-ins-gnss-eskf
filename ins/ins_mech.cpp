#include "ins_mech.h"

namespace Ins
{
    State::Pva insMech(const State::Pva& prePva,const Measurement::IMU& preImu,const Measurement::IMU& currImu)
    {

    };

    static Eigen::Vector3d posUpdate(const State::Pva& prePva,const Measurement::IMU& preImu,const Measurement::IMU& currImu)
    {

    };

    static Eigen::Vector3d velUpdate(const State::Pva& prePva,const Measurement::IMU& preImu,const Measurement::IMU& currImu)
    {

    };

    static Eigen::Quaterniond attUpdate(const State::Pva& prePva,const Measurement::IMU& preImu,const Measurement::IMU& currImu)
    {

    };
};